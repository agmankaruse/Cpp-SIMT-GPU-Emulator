#include "memory_request.hpp"

namespace simt {

// MemoryRequest is intentionally a small value type. The simulator keeps the
// behavior in StreamingMultiprocessor so request completion can update lanes,
// scoreboards, and statistics in one place.

} // namespace simt
